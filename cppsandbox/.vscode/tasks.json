{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "bazel build dbg",
            "type": "shell",
            "command": "CC=/opt/homebrew/Cellar/gcc/13.2.0/bin/g++-13 bazel build -c dbg //${relativeFileDirname}:${fileBasenameNoExtension}",
        }
    ]
}